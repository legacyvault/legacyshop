# Storefront Performance Audit — Fix Tracker

Order of work, per the audit. Check items off as they're applied and verified.

- [x] **1. DB indexes on products/product_pictures** — migration `2026_08_14_000000_add_storefront_performance_indexes.php` added to `database/migrations/`. Run `php artisan migrate` and verify with `EXPLAIN` on the product listing query.
- [x] **2. Switch cache/session to Redis** — already done: live `.env` has `CACHE_STORE=redis`, `SESSION_DRIVER=redis`, `predis/predis` installed. Only `.env.example` (the template) still says `database`; left as-is per user decision. Open question: `REDIS_HOST=127.0.0.1` — confirm Redis is co-located with the app server.
- [x] **3. Make FrontLayout a persistent Inertia layout** — done. Added `resources/js/contexts/SearchBarContext.tsx` (lets a page publish its local search state to the persistent header instead of passing it as props). Converted `FrontLayout`/`FrontHeader` to read `auth`/`locale`/`translations` from `usePage()` instead of props. Converted 9 pages to `Page.layout = (page) => <FrontLayout>{page}</FrontLayout>`: `welcome.tsx`, `front/products/index.tsx`, `front/products/product-detail.tsx`, `front/carts/index.tsx`, `front/articles/index.tsx`, `front/articles/view-article.tsx`, `profile/index.tsx` (role-aware: admin gets AppLayout, everyone else gets FrontLayout), `settings/delivery-address/index.tsx`, `settings/delivery-address/add-delivery-address.tsx`, `settings/purchases/index.tsx`. `tsc --noEmit` passes clean on the whole project. **To verify:** run the dev server, navigate between storefront pages, and confirm in the Network tab that `active.running-text` / `public.active-events` fire once per session (not per navigation), and that the header/footer/cart don't visibly flash/remount on navigation.
- [x] **4. Lazy-load product images + defer payment SDKs** — done. Added `loading="lazy"` + `width`/`height` to grid/thumbnail/cart product images (`product-card.tsx`, `product-detail.tsx` thumbnails, `carts/index.tsx`, `CartDropdown.tsx`); the single product-detail hero image got `width`/`height` only (kept eager — it's the largest above-the-fold element, lazy-loading it would hurt perceived load). Midtrans/PayPal `<script>` tags in `app.blade.php` now only render on pages that actually use them (`front/checkout/index`, `settings/purchases/index`, `orders/index`, `orders/summary/index` — found via grep for `window.snap`/`paypal` usage, not just checkout) and got `defer` added. Verified with `npx tsc --noEmit -p .` (0 errors) and `npx eslint` on all touched files (no new issues) on the user's machine. **To verify:** confirm Midtrans/PayPal network requests only fire on checkout and order-payment pages, not on home/products/articles; spot-check image `loading="lazy"` in DevTools Elements panel on the product grid.
- [~] **5. CDN + resized product thumbnails** — code done, **needs 2 manual steps before it works** (see below). Added `intervention/image` to `composer.json`; `AwsS3::uploadToS3()` now generates a ~400px JPEG thumbnail on every product picture upload and stores it in a new `thumbnail_url` column (migration `2026_08_17_000000_add_thumbnail_url_to_product_pictures_table.php`); thumbnail generation is wrapped in try/catch so a failure there never blocks the upload itself. Frontend (`product-card.tsx`, `carts/index.tsx`, `CartDropdown.tsx`, `CartContext.tsx`, `product-detail.tsx`'s cart-add) now prefers `thumbnail_url` with a fallback to the full-size `url` — the product-detail hero image intentionally still uses the full-size original. Added `AWS_S3_CDN_URL` env support in `AwsS3::buildPublicUrl()`: when set, image URLs are served from there instead of the raw S3 endpoint — leave it empty until a CDN is provisioned. TLS verification in `getS3Client()` was **not** changed — `AWS_S3_ENDPOINT` points at real AWS S3 (valid cert always), so `'verify' => false` looks unnecessary, but flip it yourself once you're sure nothing depends on it.
  **Required manual steps (I can't run these from here):**
  1. `composer require intervention/image:^3.9` (or `composer update intervention/image`) — I edited `composer.json` but can't regenerate `composer.lock`/install vendor packages remotely; deploys will fail until this runs.
  2. `php artisan migrate` — applies the new `thumbnail_url` column.
  3. Confirm the GD PHP extension is enabled on your server (nearly always is; Intervention Image needs it).
  4. Optional: add `AWS_S3_CDN_URL=` to `.env.example`/document it (I couldn't write your real `.env` — that's blocked for me — but the app works fine with it unset).
  **Note:** only *new* uploads get a thumbnail — existing product pictures keep `thumbnail_url = null` and the frontend falls back to their full-size `url`, so nothing breaks, but the grid/cart won't be lighter for existing images until they're re-uploaded (or a backfill script is run — not built, wasn't asked for).
- [~] **5b. Extend thumbnails to article/unit/event storefront images** — code done, covered by the **same 2 manual steps as #5** (no extra composer package or migration run needed beyond what's already pending — just make sure `php artisan migrate` runs *after* pulling these files, since it now also picks up 3 new migrations). Same pattern as products: `AwsS3::uploadArticleImageToS3()` / `uploadUnitImageToS3()` / `uploadEventImageToS3()` now generate a ~400px thumbnail alongside the full-size upload and return `{url, thumbnail_url}`; new nullable `thumbnail_url` columns added to `articles`, `unit`, `events` tables. **Banner intentionally excluded** — it's a full-width hero, a thumbnail wouldn't help there.
  - **Article covers** (homepage featured/secondary cards, articles listing page): prefer `thumbnail_url`, fall back to `image_cover`. The article-detail page hero image is unchanged — still full-size, same reasoning as the product-detail hero.
  - **Unit & event showcase tiles** (homepage): prefer `thumbnail_url`, fall back to `picture_url`, in the CSS `background-image`. These tiles use a CSS background (not an `<img>` tag), so they couldn't get `loading="lazy"` in fix #4 either way — this thumbnail is the only lever available for them.
  - Admin article form (`add-articles.tsx`) now captures and submits `thumbnail_url` alongside `image_cover` when a cover image is uploaded (article create/update is a separate request from the image upload, unlike unit/event which upload inline) — no dashboard changes needed for unit/event, their admin forms already upload+save in one request.
  - Verified with `npx tsc --noEmit -p .` (0 errors) and `npx eslint` on all touched files (only pre-existing lint debt already in those files, nothing new from this change).
  - **Note:** same as #5 — only *new* uploads get a thumbnail; existing articles/units/events keep `thumbnail_url = null` and fall back to their full-size image until re-uploaded.
- [ ] **6. Convert add-to-cart to a plain API call** — replace the Inertia `router.post` visit in `CartContext.addItem()` with a plain `fetch`/axios JSON call, update local cart state from the response (drop the extra `fetchServerCart()` round trip).

_Scope: storefront only (`resources/js/pages/front/*`, `layouts/front/*`, `ViewController`/`ProductController`, image pipeline, DB schema). Dashboard/admin untouched._
